# PythonTestingFrameWork
This is to demo a Pytest framework with selenium 
To run this frame work in Visual Studio Code install create a new virtual environment for python and install selenium and pytest by using 
pip install selenium
pip install pytest-html
